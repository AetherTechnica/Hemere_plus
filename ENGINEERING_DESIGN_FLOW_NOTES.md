# 人力飛行機主翼の空力・構造・積層同時設計フロー整理

作成日: 2026-05-06  
対象リポジトリ: `Hemere_plus`  
目的: ここまでの実装実験で得られた工学的知見、プログラム上の難しさ、今後の全体設計フローを整理する。

---

## 0. この文書の位置づけ

この文書は、プログラムの変更履歴そのものではなく、今回の開発で見えてきた **人力飛行機主翼設計の考え方** を整理するためのメモである。

ここで扱っている問題は、単純な「桁を軽くする」問題ではない。人力飛行機の主翼設計では、空力性能、構造重量、たわみ、応力、製作性が互いに強く結びついている。特に主翼桁は、空力的に望ましい循環分布を実現するための翼形状・荷重分布に耐えながら、機体総重量にも直接影響する。したがって、桁重量を外から固定値として与えるだけでは、設計全体の整合性が崩れる。

今回の実装実験で到達した重要な点は、以下である。

- 空力計算、構造計算、積層候補選択、桁重量更新、beta sweep を一つのループで接続できた。
- 各マンドレル径ごとの断面候補を、重量・縦曲げ剛性・前後剛性・D/t などの物理量で比較できるようになった。
- 完全列挙と実用近似探索の計算量差が明確になった。
- cap ply 上限が小さいと実行可能解に届かないことが確認できた。
- 軽い候補だけを残す探索は、たわみ制約を満たす高剛性候補を落としやすいことがわかった。
- 候補選択と beam search の両方で「軽さ」と「剛性余裕」を同時に残す必要があることがわかった。
- `max_total_cap_plies=16` の近似 frontier table を使うことで、opt-ATLAS 参照の3条件すべてで feasible な解が出た。

この段階は、最終設計ではない。むしろ、最終設計へ進むための **探索基盤が成立した段階** である。ここから先は、製作性制約、前後剛性、beta 分解能、近似探索の妥当性評価を詰める必要がある。

---

## 1. 解こうとしている設計問題

### 1.1 人力飛行機主翼で本当に最小化したいもの

人力飛行機では、最終的には必要パワーを下げたい。その中で主翼設計に関係する大きな要素は、誘導抗力と構造重量である。

翼の揚力分布を理想的な楕円分布に近づければ、誘導抗力は小さくなる。しかし、その荷重分布が構造的に厳しければ、桁を重くする必要がある。桁が重くなると機体総重量が増え、必要揚力が増え、誘導抗力も増える。

したがって、主翼の設計では次のような循環が発生する。

```text
beta を決める
  -> 揚力分布が決まる
  -> 曲げモーメント分布が決まる
  -> 必要な桁剛性が決まる
  -> 積層構成と桁重量が決まる
  -> 機体総重量が変わる
  -> 必要揚力が変わる
  -> 誘導抗力が変わる
  -> beta の良し悪しが変わる
```

このため、beta だけを空力的に最適化しても不十分であり、桁重量を固定して構造だけを最適化しても不十分である。空力と構造重量を同時に見た設計フローが必要になる。

### 1.2 beta とは何か

このプロジェクトでは、beta を翼根曲げモーメント比、または揚力分布形状を制御するパラメータとして扱っている。

直感的には、beta を変えることで、翼根側に荷重を寄せるか、翼端側に荷重を残すかが変わる。一般に、揚力分布を楕円に近づけるほど誘導抗力は低くなるが、構造的には翼端側まで荷重が残るため、スパン全体のたわみや曲げ剛性要求に影響する。

beta を下げると、荷重分布が変わり、桁重量が減る可能性がある。一方で、誘導抗力が悪化する可能性もある。beta を上げると、空力的には有利になることがあるが、構造重量が増える可能性がある。

つまり beta は、空力と構造のトレードオフを表す重要な設計変数である。

### 1.3 桁設計の難しさ

桁設計では、単に断面二次モーメントが大きければよいわけではない。

満たすべき条件は少なくとも以下である。

- 翼端たわみが許容値以下であること
- 最大応力が許容値以下であること
- 桁が薄すぎないこと、つまり D/t が大きすぎないこと
- 前後方向の剛性が不足しないこと
- スパン方向に自然な積層遷移になっていること
- 製作可能な積層順序・段落としになっていること
- 重量が過大でないこと

これらは互いに衝突する。

例えば、たわみを抑えるにはEIを上げたい。EIを上げるにはcap plyを増やすか、広いPhiを使う必要がある。しかし、それは重量増につながる。重量が増えると必要揚力が増え、曲げモーメントも増え、さらにEIが必要になる。このフィードバックがあるため、局所的に「この断面で必要EIを満たす最軽量」を選ぶだけでは、全体最適に近づかないことがある。

---

## 2. 以前の設計フローの課題

### 2.1 その場で積層を探す方式の限界

以前の統合フローでは、各スパン位置で必要EIが出たあと、そのEIを満たす積層をその場で探していた。これは直感的で実装しやすい。必要な剛性があり、その剛性を満たす最軽量の積層を探す、という流れである。

しかし、この方式には限界がある。

第一に、探索が貪欲になりやすい。局所的に軽い積層を選ぶことはできても、スパン全体で見たときに自然な段落としになるとは限らない。

第二に、探索候補が暗黙的である。ある積層が選ばれたとき、それが本当に良い候補だったのか、ほかに少し重いが大きく剛性が高い候補があったのかを比較しにくい。

第三に、毎回探索をやり直すため、beta sweep や重量収束ループの中で計算負荷が大きくなる。

第四に、積層の設計空間が大きくなると、探索が軽い側や近い側に偏り、必要な高剛性候補を見落とす可能性がある。

### 2.2 積層設計の次元の呪い

人力飛行機の主翼桁では、スパン方向に複数のセグメントを置く。今回のモデルでは8セグメントを使っている。

各セグメントごとに積層候補が数百から数千あると、全組み合わせは指数的に増える。

例えば、各セグメントに500候補あるとする。

```text
500^8 = 3.9e21
```

これは全探索できない。

したがって、候補を絞る必要がある。しかし、候補を絞りすぎると、実行可能解や良い解を落とす。ここに探索設計の難しさがある。

### 2.3 軽量化だけを見た候補削減の危険性

今回の実験で特に重要だった発見は、軽い順に候補を残すと、構造制約を満たす高剛性候補を落としやすいということである。

桁設計では、目的関数は軽量化に近い。しかし、制約はたわみと応力である。制約を満たす前に軽い候補だけを残してしまうと、最終的にはすべての組み合わせがたわみ制約を満たせない、ということが起きる。

これはプログラム上は「候補を増やしたのに良い解が出ない」という形で現れる。しかし工学的には、探索空間の残し方が間違っているという問題である。

設計候補を削減するときは、軽量候補だけでなく、高剛性候補も残す必要がある。

---

## 3. 今回導入した断面候補テーブル方式

### 3.1 断面候補テーブルの考え方

今回の大きな方向転換は、各マンドレル径ごとに断面候補を事前に作る方式である。

これは、各径に対して「この径ならこういう積層があり、それぞれ重量と剛性はこうである」というカタログを作ることに相当する。

各候補には以下の情報を持たせる。

- cap ply の Phi sequence
- 重量 `weight_kg_m`
- 縦曲げ剛性 `EI_vertical_Nmm2`
- 前後曲げ剛性 `EI_foreaft_Nmm2`
- 最小厚み `t_min_mm`
- 最大厚み `t_max_mm`
- D/t
- layer count
- cap layer count

この候補テーブルを使えば、統合設計側では毎回積層探索をやり直す必要がない。既存候補から選ぶだけでよい。

これは設計上も意味がある。なぜなら、実際の設計でも「この径の桁に対して候補となる積層パターン」を事前に作り、そこからスパン方向に組み合わせるからである。

### 3.2 候補の支配関係

候補が多すぎると探索できないため、明らかに不要な候補を消す。

例えば候補Aと候補Bがあるとする。

候補Aが候補Bより、

- 軽い
- 縦EIが大きい
- 前後EIが大きい
- D/tが小さい

なら、候補Bを選ぶ理由は基本的にない。候補Aが候補Bを支配している。

このような支配関係で候補を削ると、設計上意味のある frontier が残る。

ここで重要なのは、単一目的の最小化ではないということである。重量だけでなく、複数の物理指標を見て候補を残す必要がある。

### 3.3 exact table と frontier table

今回、2種類の table 生成方法を作った。

ひとつは exact table である。これは積層文法で許される候補をすべて列挙し、最後に非支配候補を残す。正確性が高いが、ply数が増えると計算量が爆発する。

もうひとつは frontier table である。これは探索途中で候補を削減しながら進める。完全性は落ちるが、実用的な時間でより大きい ply 数まで到達できる。

今回の実験では、実用結果を出すために frontier table を使った。

ここでの工学的判断は、最初から完全性にこだわりすぎず、まず設計フロー全体を通すことを優先した、というものである。

---

## 4. 完全列挙でわかった計算量の壁

### 4.1 cap数と候補数

積層候補数は cap ply 数が増えると急増する。

今回の実験では、おおよそ以下のような傾向が見えた。

```text
cap4  : 軽く回る
cap5  : まだ可能
cap6  : exact ではかなり重い
cap8+ : exact は実用的でない
```

これは、積層文法の自由度が大きいことを示している。

特に Phi sequence には、`Phi180` によるリセットや、集中積層 run の制約がある。これらの制約があっても、候補数は指数的に増える。

### 4.2 exact の価値

exact が重いから不要というわけではない。

exact は、近似探索がどれくらい候補を落としているかを評価する基準として重要である。小規模条件では exact を使い、近似 frontier の安全性を確認できる。

つまり exact は最終的な実用探索の主役ではなく、近似探索の検証用として使うのが現実的である。

### 4.3 実用探索への移行

cap16 まで探索しないと実行可能解が出にくいことがわかったため、exact にこだわると全体設計が進まない。そこで、実用tableは frontier 近似で作ることにした。

今回の実用tableは、以下のような性格を持つ。

- 完全列挙ではない
- prefix pruning と bin pruning を使っている
- 高速化のために候補を削っている
- ただし最終的に3条件すべてで feasible な解が出た

これは「最終精度の保証」ではなく、「設計探索フローが成立する最初の実用基盤」である。

---

## 5. cap ply 上限からわかった構造的知見

### 5.1 cap4 では不足

最初に cap4 の table を作って統合フローを試した。

cap4 では、条件3の翼根側で応力制約を満たす候補が存在しなかった。つまり、モデル上は cap4 程度の積層自由度では翼根の曲げ応力に耐えられない。

これは工学的に重要である。なぜなら、単に最適化の設定が悪いのではなく、探索空間そのものが物理的に足りないことを示しているからである。

### 5.2 cap8 でもまだ厳しい

cap8 近似 table では、翼根側の最小応力が許容値にかなり近づいたが、まだわずかに超過した。

この時点で、応力制約に対しては改善しているが、たわみ制約にはまだ十分ではなかった。

これは、応力とたわみが別の制約であることを示している。応力を満たすだけなら局所的な断面強度でよいが、たわみはスパン全体の曲率積分で決まる。翼根だけでなく、スパン全体に十分なEI分布が必要になる。

### 5.3 cap12 では初期荷重ならぎりぎり

cap12 では、初期荷重で最大EI候補を選んだ場合、条件3のたわみ許容に近づいた。

しかし、統合ループで桁自重を含めると、総重量が増え、必要揚力が増え、曲げモーメントも増える。そのため、初期荷重では足りていても、収束後には不足する。

これは、桁重量を連成することの重要性を示している。

静的に「この荷重なら大丈夫」と見ても、桁重量を入れると大丈夫ではなくなる可能性がある。

### 5.4 cap16 で全条件 feasible

cap16 近似 table では、条件1、条件2、条件3のすべてで feasible な解が出た。

結果は以下である。

```text
Condition 1:
  beta = 1.0000
  Di = 9.4299 N
  W_spar = 12.897 kg
  delta = 1161 mm / allow 2200 mm
  feasible = True

Condition 2:
  beta = 1.0000
  Di = 8.4737 N
  W_spar = 10.433 kg
  delta = 1093 mm / allow 2000 mm
  feasible = True

Condition 3:
  beta = 1.0000
  Di = 7.2729 N
  W_spar = 6.822 kg
  delta = 809 mm / allow 900 mm
  feasible = True
```

この結果から、少なくとも現在のモデル・材料・径分布・制約では、cap16程度の探索余地を持つと feasible な設計案が得られることがわかった。

ただし、これは最終設計ではない。近似探索であり、製作性制約はまだ十分に入っていない。

---

## 6. 候補選択で得られた重要知見

### 6.1 軽い候補だけでは駄目

今回、候補数を制限する際に、最初は軽い順で候補を残していた。

これは一見合理的である。最終的には桁重量を減らしたいからである。

しかし実際には、軽い候補だけを残すと、たわみ制約を満たす高剛性候補が落ちる。結果として、table の中には実行可能な候補があるのに、最適化がそれを使えないという状況が発生した。

これは構造最適化ではよくある問題である。

目的関数は重量だが、制約は剛性である。制約を満たす前に重量だけで候補を切ると、実行可能領域そのものを失う。

### 6.2 EI多様性を残す必要

この問題に対して、候補選択に EI diversity を入れた。

これは、EIを対数的な範囲で分割し、各EIレンジから代表候補を残す考え方である。

つまり、次のような候補を同時に残す。

- とても軽いが剛性が低い候補
- 中程度の重量と剛性の候補
- 重いが高剛性な候補

この変更により、探索は実行可能解に到達しやすくなった。

工学的には、これは設計検討で複数案を残すことに相当する。

実際の設計でも、最初から最軽量案だけを見るのではなく、剛性余裕のある案、製作しやすい案、安全率の高い案を比較する。プログラム上でも同じことが必要だった。

### 6.3 table圧縮と frontier records の違い

table には、圧縮された `table_entries` と、より広い `frontier_candidates` がある。

`table_entries` は必要EIに対する lookup には便利だが、span optimizer の組み合わせ探索には候補が薄すぎる場合がある。

今回の統合 sweep では、`frontier_records` を使う方が安全だった。

これは重要である。将来的に高速化したい場合、単純なEI lookupだけでなく、応力・たわみ・段落とし制約を考慮した候補集合が必要になる。

---

## 7. beam search で得られた重要知見

### 7.1 beam search の役割

8セグメントの候補組み合わせは非常に多い。全組み合わせを試すことはできないため、beam search を使う。

beam search は、スパン方向に候補を一つずつ追加しながら、有望な途中状態だけを残す方法である。

しかし、何をもって「有望」とするかが難しい。

### 7.2 重量順beamの問題

最初は、途中状態を重量順に残していた。

これは最終目的が軽量化であることを考えると自然に見える。しかし、実際にはこれも軽量候補偏重の問題を起こした。

途中で軽い状態だけを残すと、後でたわみ制約を満たすために必要な高剛性状態が消えてしまう。

特にたわみはスパン全体の曲率積分で決まるため、途中段階では少し重くても剛性が高い状態を残す必要がある。

### 7.3 コンプライアンスを使ったbeam多様化

そこで beam search の中でも、以下の2種類を残すようにした。

- 軽い状態
- コンプライアンスが小さい、つまり硬い状態

コンプライアンスは、おおまかには曲げモーメントをEIで割った量の積分的な指標である。小さいほどたわみにくい。

この変更により、最終的に実行可能な高剛性組み合わせが beam の中に残りやすくなった。

工学的には、探索途中で「軽い案」と「硬い案」の両方を残すことに相当する。

---

## 8. 今回出た3条件の結果の意味

### 8.1 条件1

条件1は、スパン32.5m、速度7.2m/s の低速ロングスパン条件である。

今回の結果は以下だった。

```text
beta = 1.0000
Di = 9.4299 N
W_spar = 12.897 kg
W_total = 98.897 kg
delta = 1161 mm
allow = 2200 mm
feasible = True
```

この条件では、たわみ許容にかなり余裕がある。許容2200mmに対して1161mmなので、構造的にはまだ余裕がある。

一方で、桁重量は12.9kgと3条件の中で最も重い。スパンが長いため、構造重量が増えやすいのは自然である。

betaが1.0に寄っているのは、今回の粗いbeta範囲と候補条件では、空力的にbetaを下げるメリットよりも、beta=1.0付近の誘導抗力低減の方が勝っている可能性がある。

ただし、beta分解能が粗いため、真の最適が1.0とは限らない。0.99から1.01近辺を細かく見る必要がある。

### 8.2 条件2

条件2は、スパン29m、速度8.3m/s の中間条件である。

```text
beta = 1.0000
Di = 8.4737 N
W_spar = 10.433 kg
W_total = 96.433 kg
delta = 1093 mm
allow = 2000 mm
feasible = True
```

条件2もたわみには余裕がある。許容2000mmに対して1093mmである。

条件1よりスパンが短く、速度が高いため、桁重量はやや軽い。

opt-ATLAS参照のDiに対しては+10.5%程度大きい。これは、桁重量込みの総重量差、積層候補の制約、beta分解能、空力モデルの参照差などが影響している可能性がある。

### 8.3 条件3

条件3は、スパン22m、速度11m/s の高速ショートスパン条件である。

```text
beta = 1.0000
Di = 7.2729 N
W_spar = 6.822 kg
W_total = 89.822 kg
delta = 809 mm
allow = 900 mm
feasible = True
```

この条件は、たわみ許容にかなり近い。809mm / 900mm なので、余裕はあるが大きくはない。

cap数を増やしていく過程でも、条件3は最も厳しい検証条件として機能した。cap4, cap8, cap9, cap12 では十分な実行可能解に届かず、cap16でようやく安定した。

したがって、今後の探索設定や制約追加では、条件3を代表的な厳しいケースとして使うとよい。

---

## 9. 今回の結果をどう解釈すべきか

### 9.1 これは最終設計ではない

今回の結果は「設計完了」ではない。

理由は以下である。

- 近似 frontier table を使っている。
- beta sweep が9点で粗い。
- 段落とし制約は全条件sweepではOFFである。
- cap sequence に製作上不自然な並びが残っている。
- 前後剛性制約の現実値が未設定である。
- 近似 pruning の安全性評価はまだ不十分である。
- opt-ATLAS参照との差が条件によって残っている。

つまり、今回の結果は **設計フローが成立した基準解** であり、最終設計案ではない。

### 9.2 それでも重要な理由

重要なのは、これで空力・構造・積層・重量収束が一つのループで動き、実行可能解が出るようになったことである。

これは設計探索の土台である。

今後、制約を追加したり、betaを細かくしたり、製作性を高めたりしても、比較対象となる基準解がある。

基準解がない状態では、改善したのか悪化したのか判断できない。今回の結果により、今後の改善を定量的に比較できるようになった。

---

## 10. 製作性の問題

### 10.1 現在の cap sequence の不自然さ

今回の結果を見ると、cap sequence には `Phi180` が途中に入るものがある。

`Phi180` は全周0度層を意味し、文法上は集中積層runをリセットする役割を持つ。数学的には候補として有効だが、製作上そのまま自然かどうかは別問題である。

また、rootからtipに向かってplyが滑らかに減るとは限らない。あるセグメントで消えたような積層が外側で再び出る可能性もある。

これは、現時点の探索が「断面ごとの物理性能」を中心にしていて、スパン方向の製作手順を十分に見ていないためである。

### 10.2 段落とし制約の必要性

実際の桁では、rootからtipに向かってcap plyが自然に減っていくことが望ましい。

考えるべき制約は以下である。

- rootからtipに向かってcap ply数が増えない。
- 隣接セクションで急激なPhi変化を避ける。
- 一度落としたplyが外側で再出現しない。
- ply drop の間隔を確保する。
- 段差が集中しないようにする。

今回、プログラム上は段落とし制約を入れられるようになった。ただし、全条件sweepの最終結果ではOFFである。

次の重要な作業は、この段落とし制約をONにして、重量増と実行可能性の変化を見ることである。

### 10.3 製作性を入れると何が起きるか

段落とし制約を入れると、候補の自由度は下がる。

その結果、以下が起きる可能性がある。

- 重量が増える。
- たわみが増える。
- 条件3が infeasible になる。
- beta の最適値が変わる。
- cap数上限をさらに増やす必要が出る。

これは悪いことではない。むしろ、製作可能性を入れた現実的な設計に近づくということである。

---

## 11. 前後剛性の位置づけ

### 11.1 縦曲げだけでは不十分

現在の主な制約は縦曲げEI、たわみ、応力である。

しかし実機の桁では、前後方向の剛性も重要である。

前後剛性が不足すると、以下のような問題が考えられる。

- 運搬時や地上取り扱いで曲がりやすい。
- 着陸・接地・支持時の荷重に弱い。
- 翼のねじれや局所変形に影響する。
- リブや外皮との相互作用で問題が出る。

今回の断面計算では `EI_foreaft` を計算できるようになっている。これは大きな進展である。

### 11.2 まだ足りないもの

ただし、前後剛性の要求値はまだ決まっていない。

暫定的には以下のような制約が考えられる。

```text
EI_foreaft >= ratio * EI_vertical
```

しかし、これは便宜的な制約である。

本来は、運搬荷重、着地荷重、支持点条件、リブ配置、外皮との接続などから前後方向の要求を決めるべきである。

したがって、今後の全体設計フローでは、前後剛性要求をどのレベルで入れるかを決める必要がある。

---

## 12. D/t と座屈的な見方

### 12.1 D/t の意味

D/t は、桁径に対する厚みの比である。

D/t が大きいほど、薄肉である。薄肉構造は軽いが、局所座屈や取り扱い上の弱さが問題になりやすい。

今回の table では D/t を候補ごとに計算している。これにより、剛性だけでなく薄すぎる候補を除外できる。

### 12.2 現時点の扱い

今回の最終 sweep では、D/t 制約を強く入れていない。

これは、まず実行可能フローを通すことを優先したためである。

次の段階では、D/t 上限を現実的に設定し、結果がどう変わるかを見る必要がある。

一般にD/t制約を厳しくすると、薄くて軽い候補が落ちる。そのため重量は増える可能性がある。一方で、実機としては安全で扱いやすくなる。

---

## 13. beta 最適化について

### 13.1 今回のbeta sweepは粗い

今回の全条件sweepでは、betaを以下の範囲で9点だけ見た。

```text
0.97, 0.975, 0.98, 0.985, 0.99, 0.995, 1.0, 1.005, 1.01
```

結果として、3条件すべてで beta=1.0 が選ばれた。

これは興味深いが、このまま「beta=1.0が最適」と結論するのは早い。

理由は以下である。

- 分解能が粗い。
- 積層候補が近似である。
- 製作性制約がまだOFFである。
- 桁重量と空力の相互作用が粗く評価されている。

### 13.2 次に見るべきbeta範囲

次は beta=0.99 から 1.01 の範囲を細かく見るのがよい。

例えば、

```text
beta_min = 0.985
beta_max = 1.01
n_beta = 26
```

のようにすれば、0.001程度の分解能で見られる。

ただし、実行時間が重いので、まず条件3だけで試すのがよい。条件3はたわみ制約が最も厳しく、設計感度が見えやすい。

### 13.3 beta と構造重量の関係

beta を変えると、誘導抗力だけでなく桁重量も変わる。

したがって、評価すべき指標は Di だけではない。

見るべきものは以下である。

- beta
- Di
- W_spar
- W_total
- delta_tip
- sigma
- selected cap sequence
- feasibility

beta を下げてDiが少し悪化しても、桁重量が大きく下がるなら、総合的には有利になる可能性がある。逆に、Diが良くても桁重量が増えすぎるなら不利になる可能性がある。

---

## 14. opt-ATLAS参照との差の扱い

### 14.1 差があること自体は自然

今回の結果では、opt-ATLAS参照値に対してDiの差が残っている。

これは必ずしもプログラムのバグとは限らない。

理由は以下である。

- opt-ATLASとHemereでは桁重量の扱いが異なる。
- 積層モデルが異なる。
- Hemereは集中積層や非全周積層を扱っている。
- beta sweepの分解能が違う。
- 参照値が桁自重をどう扱っているかが完全には一致しない。

したがって、opt-ATLASとの差は「完全一致すべき値」ではなく、モデル差を理解するための基準として扱うべきである。

### 14.2 比較すべき観点

opt-ATLASとの比較では、以下を分けて見る必要がある。

1. 空力計算単体が一致しているか
2. 同じ重量・同じbetaでDiが一致しているか
3. 桁重量込みで最適betaがどう変わるか
4. 桁重量がどれくらい違うか
5. 積層制約によりどれくらい重量が増えるか

今回の実装では、空力・構造・積層が統合されているため、単純にDiだけを見ると原因が混ざる。

今後は、比較用のレポートで各要因を分解する必要がある。

---

## 15. 全体設計フロー案

ここまでの知見を踏まえると、人力飛行機主翼の全体設計フローは以下のように整理できる。

### 15.1 Step 1: 基本条件を決める

まず、以下の設計条件を決める。

- スパン
- 巡航速度
- パイロット重量
- 胴体重量
- 主翼二次構造重量
- 許容たわみ
- 許容応力
- マンドレル径分布
- セグメント数
- 空力計算点数

この段階では、桁積層はまだ決めない。

### 15.2 Step 2: 積層文法を決める

次に、どのような積層を許すかを決める。

現在の文法はおおまかに以下である。

```text
inner 90
+45
-45
cap 0 layers with Phi sequence
outer 90
```

今後は、製作性を考慮して文法をさらに絞る必要がある。

例えば、

- Phiの刻み
- Phi180の扱い
- 最大cap数
- 集中runの上限
- 段落とし規則

を決める。

### 15.3 Step 3: 各径の断面候補tableを作る

各マンドレル径ごとに候補tableを作る。

候補tableには、重量・EI・D/t・cap sequenceなどを保存する。

この段階で重要なのは、最終探索に必要な高剛性候補を落としすぎないことである。

軽量候補だけでなく、EIレンジ全体の候補を残す。

### 15.4 Step 4: tableの健全性を診断する

tableを作ったら、以下を確認する。

- 各径で候補数が十分あるか
- EIレンジが必要な範囲を覆っているか
- D/tが極端でないか
- 重量レンジが妥当か
- 代表cap sequenceが製作上おかしくないか
- 条件3の翼根で応力制約を満たす候補が存在するか
- 最大EI候補でたわみ制約に届くか

この診断をしないまま統合設計に入ると、結果が infeasible になったときに原因がわからない。

### 15.5 Step 5: table-backed beta sweepを実行する

tableを使って、beta sweepを行う。

各betaで、

```text
W_totalを仮定
  -> 空力計算
  -> 曲げモーメント計算
  -> 各スパン位置の候補選択
  -> 桁重量計算
  -> W_total更新
  -> 収束まで繰り返し
```

を行う。

最後に、feasibleなbetaの中からDiが最小のものを選ぶ。

### 15.6 Step 6: 積層遷移を確認する

得られた解について、各スパン位置のcap sequenceを確認する。

見るべきポイントは以下である。

- rootからtipに向かって自然にplyが減っているか
- Phiが急に変化していないか
- 消えたplyが外側で再出現していないか
- `Phi180` が不自然な位置に入っていないか
- 実際に巻ける順序になっているか

ここで不自然なら、文法や段落とし制約を修正して再探索する。

### 15.7 Step 7: 感度解析

基準解が出たら、以下を変えて感度を見る。

- beta分解能
- cap数上限
- phi刻み
- D/t制約
- 前後剛性制約
- 段落とし制約
- beam幅
- EI diversity bins

これにより、結果が探索設定にどれくらい依存しているかがわかる。

### 15.8 Step 8: 最終設計候補を選ぶ

最終的には、単にDiが最小の解ではなく、以下を総合して選ぶべきである。

- Di
- W_spar
- W_total
- たわみ余裕
- 応力余裕
- D/t余裕
- 前後剛性余裕
- 製作しやすさ
- 段落としの自然さ
- 設計変更への余裕

人力飛行機の設計では、数値上の最適解がそのまま実機最適とは限らない。軽さと安全性、製作性のバランスが重要である。

---

## 16. プログラム的な難しさ

### 16.1 データ量と計算時間

積層候補は非常に多い。cap数を増やすだけで計算時間が急増する。

今回も cap16 table を作るのにかなり時間がかかった。全条件分のtable生成には長時間を要した。

このため、今後は以下が重要になる。

- table生成の並列化
- 途中結果保存
- 既存tableの再利用
- 不要なdebug patchを保存しない
- 近似pruningの安全性評価
- 生成済みtableのmanifest管理

### 16.2 候補削減の難しさ

候補を削らないと計算できない。しかし削り方を間違えると良い解が消える。

今回、軽さ順の候補削減が危険であることがわかった。

そのため、候補削減は複数の観点で行う必要がある。

- 重量
- EI
- D/t
- 応力余裕
- 前後剛性
- 製作性

### 16.3 JSON tableの扱い

tableを保存して再利用するため、JSON manifestを導入した。

これにより、生成済みtableを使って統合sweepを再実行できる。

ただし、manifestの直径リストと条件側の直径リストが一致しないと、意図せず frontier 生成にフォールバックする危険がある。今回のツールでは、tableに必要径がない場合は原則エラーにするようにした。

これは重要である。設計結果がtable由来なのか、その場生成由来なのかが混ざると、比較が難しくなる。

### 16.4 数値モデルと探索モデルの結合

空力計算、構造計算、候補選択、重量収束はそれぞれ別の性質を持つ。

空力計算は連続的で、betaに対して比較的滑らかである。

積層選択は離散的で、少し条件が変わるだけで選ばれる候補が飛ぶ。

重量収束は、桁重量が変わると荷重が変わり、また桁重量が変わるという固定点問題である。

このため、全体の応答は滑らかとは限らない。

beta sweepで局所的な飛びが出る可能性があるため、結果の解釈には注意が必要である。

---

## 17. 今後の優先タスク

### 17.1 段落とし制約ONの再実行

次にやるべき最重要タスクは、段落とし制約をONにして cap16 table sweep を再実行することである。

候補設定例:

```text
enforce_monotone_cap_plies = True
require_common_phi_nonincreasing = True
max_cap_ply_drop_per_transition = 2
max_phi_change_deg_per_transition = 80
```

まずは緩めに入れる。

その後、

```text
max_cap_ply_drop_per_transition = 1
max_phi_change_deg_per_transition = 60
```

のように厳しくする。

見るべき指標は以下である。

- feasibleが維持されるか
- W_sparがどれくらい増えるか
- deltaがどれくらい悪化するか
- Diがどれくらい変わるか
- cap sequenceが自然になるか

### 17.2 beta細分化

現在は9点beta sweepである。

次は条件3だけで、betaを細かく見る。

例:

```text
beta_min = 0.985
beta_max = 1.01
n_beta = 26
```

これにより、beta=1.0周辺の最適が本当にどこにあるかを確認できる。

### 17.3 前後剛性制約の導入

次に、`min_foreaft_ratio` を段階的に入れる。

例:

```text
EI_foreaft >= 0.2 * EI_vertical
EI_foreaft >= 0.3 * EI_vertical
EI_foreaft >= 0.5 * EI_vertical
```

これにより、前後剛性要求を入れたときの重量増を把握できる。

### 17.4 D/t制約

D/tの上限も段階的に見る。

例えば、

```text
max_d_over_t = 150
max_d_over_t = 120
max_d_over_t = 100
```

を試す。

薄肉候補が落ちるため、特に翼端側の候補が変わる可能性がある。

### 17.5 近似pruningの検証

cap16 exact は重すぎるため、同じ条件で完全検証するのは現実的ではない。

しかし、小さい設定では exact と approximate を比較できる。

例えば、

```text
cap4 exact vs cap4 approximate
cap5 exact vs cap5 approximate
phi_step=40 exact vs approximate
```

を比較し、近似pruningがどの程度frontierを落とすか、最終span結果に差が出るかを見る。

---

## 18. 人力飛行機設計フローとしてのまとめ

今回の実装実験から、人力飛行機主翼の設計フローは次の形に整理できる。

```text
1. 設計条件を決める
   - span
   - speed
   - fixed weight
   - deflection allow
   - stress allow
   - mandrel diameters

2. 積層文法を決める
   - base layers
   - cap Phi candidates
   - max cap plies
   - manufacturing rules

3. 各径の断面候補tableを作る
   - weight
   - EI_vertical
   - EI_foreaft
   - D/t
   - cap sequence

4. tableを診断する
   - EI range
   - stress feasibility
   - D/t range
   - representative layups

5. beta sweepを実行する
   - aero
   - structure
   - span optimizer
   - weight convergence

6. feasible解を選ぶ
   - Di minimum among feasible
   - W_spar
   - delta
   - stress

7. 製作性を確認する
   - ply drop
   - cap sequence
   - monotonic transition

8. 制約を追加して再探索する
   - fore-aft stiffness
   - D/t
   - manufacturing

9. 感度解析する
   - beta resolution
   - cap limit
   - phi step
   - pruning settings

10. 最終候補を選ぶ
   - performance
   - weight
   - safety
   - manufacturability
```

このフローの中で重要なのは、最初から完全な最適解を狙わないことである。

まず実行可能な基準解を作る。次に制約を足す。制約による重量増や性能低下を測る。最後に、性能と製作性のバランスで選ぶ。

今回の開発は、このうち 3 から 6 までを実際に動かせる状態にしたと言える。

---

## 19. 今回得られた最重要知見

今回の開発で得られた最重要知見をまとめる。

### 知見1: 桁設計は局所EI最小重量問題ではない

各断面で必要EIを満たす最軽量候補を選ぶだけでは不十分である。

スパン全体のたわみ、応力、重量収束、製作性を同時に見る必要がある。

### 知見2: 候補削減は軽さだけでは危険

軽い候補だけを残すと、実行可能解に必要な高剛性候補を落とす。

EI diversity を残すことが重要である。

### 知見3: beam searchも剛性側の多様性が必要

途中状態を軽さ順だけで残すと、たわみ制約を満たせない軽量案に探索が偏る。

軽い状態と硬い状態の両方を残す必要がある。

### 知見4: cap数上限は設計可能性そのものを決める

cap4やcap8では、条件3で実行可能解に届かない。

現在のモデルでは、cap16程度の探索余地で全条件feasibleに到達した。

### 知見5: exact探索は検証用、実用探索は近似frontier

完全列挙は重要だが、cap数が増えると実用的でない。

実機スケールの統合設計では、近似frontier tableを使い、小規模exactで妥当性を確認するのが現実的である。

### 知見6: 条件3は厳しい代表ケースとして有用

条件3はたわみ許容が厳しく、cap数不足や候補選択の問題が顕在化しやすい。

今後の制約追加では、まず条件3で試すのが効率的である。

### 知見7: 製作性制約を入れる前の基準解ができた

cap16近似tableで3条件feasibleな基準解が得られた。

これにより、段落とし制約や前後剛性制約を入れたときの影響を定量比較できる。

---

## 20. 最後に

今回の実装実験は、単にプログラム機能を増やしただけではない。

人力飛行機の主翼設計において、空力と構造重量を連成させ、離散的な積層候補を用いて実行可能解を探索するための骨格を作った。

特に重要なのは、探索を進める中で、何が設計上の本質的な難しさなのかが見えたことである。

- 候補空間が大きすぎる。
- 完全列挙は重すぎる。
- しかし削りすぎると実行可能解を落とす。
- 軽量化だけで候補を残すと構造制約を満たせない。
- 桁自重を入れると必要剛性がさらに変わる。
- 製作性を入れると、数学的最適解はさらに制限される。

これらは、単なるプログラム上の問題ではなく、人力飛行機の実機設計そのものに関わる問題である。

今後は、この基準解を出発点として、製作可能な積層遷移、前後剛性、座屈的制約、beta細分化を順に追加し、最終的に「性能が良く、軽く、作れる」主翼桁設計へ近づけていく。

今回の到達点は、次の一文に集約できる。

**空力・構造・積層・重量収束を接続した主翼設計探索が、実行可能解を出せる段階に到達した。**

