# Hemere_plus 空力-構造同時最適化 引き継ぎ

作成日: 2026-05-05

この文書は、新しい Codex セッションへ作業を引き継ぐためのメモです。既存の `Hemere_plus` はアイデアと式の宝庫として扱い、値の信頼性は新規の独立検証ツールで担保する方針です。

## 現在の方針

主翼の空力-構造同時最適化は、以下の階層構造で実装する。

```text
LaminateGrammar
  -> 製作可能な Phi 積層列を生成

SectionCalculator
  -> 順序付き積層列から断面性能を計算

SectionFrontierBuilder
  -> 直径ごとの非支配断面候補を生成

SpanOptimizer
  -> 各スパン区間で候補を選び、たわみ制約下で最小重量化

AeroStructOptimizer
  -> beta sweep / 空力解析 / 構造最適化 / 重量収束を統合
```

Surrogate モデルを主役にせず、物理計算 + 離散探索 + pruning を中核にする。

## 実装済みファイル

### 型定義

`src/core/section_types.py`

主な定義:

- `LayerKind`
- `Material`
- `MaterialLibrary`
- `LayerSpec`
- `LaminateStack`
- `ResolvedLayer`
- `AngularPatch`
- `SectionResult`
- `StressResult`
- `SectionCalculatorConfig`

単位系:

- 長さ: `mm`
- 力: `N`
- 曲げ剛性: `N*mm^2`
- 応力: `MPa`
- 線密度: `kg/m`

`kgf*mm^2` は互換プロパティとしてのみ提供。

### 断面計算器

`src/core/section_calculator.py`

入力は従来の `ply_counts` ではなく、順序付き `LaminateStack`。

例:

```python
LaminateStack((
    LayerSpec(LayerKind.INNER_90),
    LayerSpec(LayerKind.BIAS_PLUS_45),
    LayerSpec(LayerKind.BIAS_MINUS_45),
    LayerSpec(LayerKind.CAP_0, phi_deg=170),
    LayerSpec(LayerKind.CAP_0, phi_deg=160),
    LayerSpec(LayerKind.CAP_0, phi_deg=180),
    LayerSpec(LayerKind.CAP_0, phi_deg=100),
    LayerSpec(LayerKind.OUTER_90),
))
```

実装内容:

- 各層を角度区間ごとの `AngularPatch` に分解
- 内側から半径を積み上げる
- 集中積層後に全周層を巻く非一様厚みを扱える
- `EI_vertical`
- `EI_foreaft`
- 重量
- `t_min`, `t_max`
- 保守的 `D/t`
- 垂直曲げ応力 helper

閉形式:

```text
phi = Phi [rad]

I_vertical = (Do^4 - Di^4) * (phi + sin(phi)) / 64
I_foreaft  = (Do^4 - Di^4) * (phi - sin(phi)) / 64
Area       = (Do^2 - Di^2) * phi / 4
```

`Phi180` は全周0度層。

### 積層文法

`src/core/laminate_grammar.py`

文法:

```text
[90 / +45 / -45 / CapStack / 90]
```

制約:

- `Phi180` は全周0度層
- `Phi180` は集中積層 run を reset
- `Phi < 180` は同一 run 内で単調非増加
- `Phi < 180` の連続は最大 `max_concentrated_run`
- 総 cap ply 数は `max_total_cap_plies`
- 刻みは `phi_step_deg`

初期 config 案:

```python
LaminateGrammarConfig(
    phi_min_deg=20,
    phi_max_deg=180,
    phi_step_deg=20,
    max_concentrated_run=10,
    max_total_cap_plies=40,
)
```

有効例:

```text
Phi170/Phi160/Phi140/Phi100/Phi90/Phi80/Phi70/Phi60/Phi50/Phi40
/Phi180
/Phi100/Phi70/Phi50/Phi30
/Phi180
/Phi100/Phi70/Phi50/Phi30
```

無効例:

```text
Phi50/Phi40/Phi60
```

理由: 同一 run 内で Phi が増加している。

### 断面候補 frontier

`src/core/section_frontier.py`

役割:

- 文法から候補積層列を生成
- `SectionCalculator` で断面性能を計算
- 支配解を除去

支配判定:

```text
A.weight <= B.weight
A.EI_vertical >= B.EI_vertical
A.EI_foreaft >= B.EI_foreaft
A.D/t <= B.D/t
```

重要な注意:

prefix 段階で強く pruning すると、後で `Phi180` を巻いた後に非支配解へ復活する候補を失うケースがある。そのため、既定では `approximate_prefix_pruning=False`, `approximate_bin_pruning=False` として安全側にしている。

実用規模では計算量が増えるため、近似 pruning を有効化する場合は検証が必要。

### スパン方向最適化

`src/core/span_optimizer.py`

役割:

- 各スパン区間で候補断面を1つ選ぶ
- 桁重量を最小化
- たわみ制約、応力制約、`D/t` 制約、前後剛性比制約を扱う

小規模では全探索、大規模では beam search に切り替える。

現在の制約:

```text
tip deflection <= delta_allow
stress <= sigma_allow
D/t <= max_d_over_t
EI_foreaft >= min_foreaft_ratio * EI_vertical
```

隣接セクション間の段落とし制約は未実装。

### 空力-構造統合器

`src/integration/aero_struct_optimizer.py`

役割:

- 既存 `AerodynamicsAnalyzer`
- 既存 `StructuralAnalyzer`
- 新規 `SectionFrontierBuilder`
- 新規 `SpanOptimizer`

を接続する初期統合器。

流れ:

```text
beta を決める
  -> 空力解析
  -> net load
  -> 曲げモーメント
  -> span optimizer
  -> W_spar
  -> W_total 更新
  -> 収束まで反復
```

現状は `beta` 1変数 sweep のみ。

## 検証ツール

新規に `tools` 配下へ独立検証ツールを追加済み。

```text
tools/verify_section_calculator.py
tools/verify_laminate_grammar.py
tools/verify_section_frontier.py
tools/verify_span_optimizer.py
```

実行コマンド:

```powershell
C:\Users\Shidw\anaconda3\python.exe -m compileall src tools
C:\Users\Shidw\anaconda3\python.exe tools\verify_section_calculator.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_laminate_grammar.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_section_frontier.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_span_optimizer.py
```

2026-05-05 時点ですべて PASS。

小規模 smoke test:

```powershell
@'
import numpy as np
from src.integration.aero_struct_optimizer import AeroStructDesignParams, AeroStructOptimizer, AeroStructOptimizerConfig
from src.core.laminate_grammar import LaminateGrammarConfig
from src.core.section_frontier import FrontierConfig

params = AeroStructDesignParams(
    W_pilot_kg=55.0,
    W_fuselage_kg=20.0,
    W_wing_secondary_kg=8.0,
    span_m=10.0,
    V_flight_ms=10.0,
    delta_allow_m=1.0,
    sigma_allow_MPa=300.0,
    mandrel_diameters_mm=(80.0, 60.0),
    n_spar=2,
    n_aero=20,
)
config = AeroStructOptimizerConfig(
    beta_min=0.95,
    beta_max=0.97,
    n_beta=2,
    max_weight_iter=2,
    grammar_config=LaminateGrammarConfig(phi_step_deg=60, max_total_cap_plies=2),
    frontier_config=FrontierConfig(max_states_per_depth=200, max_frontier_size=200),
    max_candidates_per_station=30,
    beam_width=100,
)
result = AeroStructOptimizer(params, config).optimize(verbose=False)
print(result.best.beta, result.best.induced_drag_N, result.best.W_spar_kg, result.best.feasible)
'@ | C:\Users\Shidw\anaconda3\python.exe -
```

確認出力:

```text
0.97 37.73263063916498 1.7903465427951342 True
```

## 次にやるべきこと

### 1. 実機スケールでの frontier 計算量確認

まず探索設定を軽くして、実機に近い直径分布で frontier サイズと計算時間を見る。

確認項目:

- 各直径の frontier 候補数
- frontier 生成時間
- 選ばれる cap sequence
- `EI_vertical`, `EI_foreaft`, `D/t`, 重量の分布

調整候補:

- `max_total_cap_plies`
- `phi_step_deg`
- `max_states_per_depth`
- `max_frontier_size`
- `approximate_prefix_pruning`
- `approximate_bin_pruning`
- `log_ei_bin_width`

### 2. frontier 近似 pruning の安全性評価

現状は安全側で pruning が弱い。

実務上は計算量削減が必要になる可能性が高いので、小規模全探索で以下を比較する。

```text
exact frontier
approximate_prefix_pruning=True
approximate_bin_pruning=True
```

見るべき指標:

- 非支配解の取りこぼし
- 最終 span optimizer の重量差
- たわみ制約に対する margin

### 3. 段落とし制約の実装

現在の `SpanOptimizer` は、隣接セクション間の製作制約を見ていない。

追加したい制約:

- root から tip に向かって cap ply が自然に減る
- 隣接セクションで急激な Phi 変化を禁止
- ply drop の最小間隔
- あるセクションに存在する cap layer が、外側セクションで不自然に再出現しない

実装候補:

```python
def transition_allowed(prev: SectionCandidate, next: SectionCandidate) -> bool:
    ...
```

beam search の遷移時にこの関数を適用する。

### 4. 前後剛性制約の現実値設定

`EI_foreaft` は計算できるようになったが、要求値は未定。

当面の候補:

```text
EI_foreaft >= ratio * EI_vertical
```

ただし、物理的には運搬荷重・着地荷重・取り扱い荷重などから要求値を決めるべき。

### 5. 応力評価の改善

現在の応力 helper は垂直曲げのみ。

初期式:

```text
curvature = M / EI_vertical
sigma = E_layer * curvature * z
```

今後:

- foreaft bending
- combined bending
- local stress at cap edges
- material allowable by layer kind

を検討。

### 6. 既存 `DesignIntegratorV3` との比較

新統合器はまだ研究用第1版。

比較項目:

- 同一条件の `beta*`
- `Di`
- `W_spar`
- `delta_tip`
- 選ばれる積層
- 実行時間

ただし、既存 `Hemere_plus` は値がおかしい疑いがあるため、差異があること自体は問題ではない。差異の理由を診断する。

## 重要な設計判断

### 既存 `SparCalculator` との差

既存:

```text
ply_counts[11] に集約
積層順序を失う
固定テンプレート前提
```

新規:

```text
ordered LaminateStack
Phi180 reset を表現可能
非一様厚みを角度区間ごとに追跡
EI_vertical / EI_foreaft を同時算出
```

今回の文法では積層順序が重要なので、既存 `ply_counts` 形式には戻さないこと。

### Phi の意味

この実装では:

```text
Phi180 = 全周0度層
Phi20  = 上下それぞれ20deg幅の集中0度層
```

閉形式もこの定義に合わせている。

### pruning の注意

prefix が現時点で支配されていても、後続の全周層や集中層の順序効果で最終的に非支配になる場合がある。

そのため、prefix pruning は安易に強くしない。

## Git 状態

2026-05-05 時点で追加された新規ファイル:

```text
src/core/laminate_grammar.py
src/core/section_calculator.py
src/core/section_frontier.py
src/core/section_types.py
src/core/span_optimizer.py
src/integration/aero_struct_optimizer.py
tools/verify_section_calculator.py
tools/verify_laminate_grammar.py
tools/verify_section_frontier.py
tools/verify_span_optimizer.py
引き継ぎ.md
```

既存ファイルは変更していない。

## 推奨される次セッション最初の手順

1. 状態確認

```powershell
cd C:\Users\Shidw\HPA\Hemere_plus
git status --short
```

2. 検証再実行

```powershell
C:\Users\Shidw\anaconda3\python.exe -m compileall src tools
C:\Users\Shidw\anaconda3\python.exe tools\verify_section_calculator.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_laminate_grammar.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_section_frontier.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_span_optimizer.py
```

3. 実機スケール frontier 診断ツールを作る

候補ファイル:

```text
tools/diagnose_frontier_runtime.py
```

出力したい内容:

```text
diameter
frontier size
generation time
min/max EI_vertical
min/max EI_foreaft
min/max weight
representative cap_phis
```

これが次の最優先タスク。

## 2026-05-05 追記: frontier 診断ツール実装後の引き継ぎ

この追記が現在最新の状態。

### 今回追加したもの

`tools/diagnose_frontier_runtime.py`

目的:

- 実機に近いマンドレル直径分布で `SectionFrontierBuilder` の計算時間と frontier サイズを見る
- 各直径の `EI_vertical`, `EI_foreaft`, `D/t`, 重量のレンジを見る
- 代表的な `cap_phis` を確認する
- 近似 pruning の安全性評価に進む前の基準データを取る

既定の実行条件:

```text
diameters = 120.0 -> 45.0 mm, 8 points
phi_min_deg = 20
phi_step_deg = 20
max_total_cap_plies = 4
max_concentrated_run = 10
max_states_per_depth = 400
max_frontier_size = 800
approximate_prefix_pruning = False
approximate_bin_pruning = False
```

注意:

- `max_total_cap_plies=8` の既定実行は 120 秒で終わらなかったため、初回診断用の既定値は `4` に下げた。
- `max_total_cap_plies=4` では、8 直径点の診断が約 23 秒で完走した。
- frontier サイズはおおむね `495-528` 候補。
- 既定値は実機最終探索ではなく、計算量を測るための軽量診断設定。

実行コマンド:

```powershell
C:\Users\Shidw\anaconda3\python.exe tools\diagnose_frontier_runtime.py
```

軽量 smoke:

```powershell
C:\Users\Shidw\anaconda3\python.exe tools\diagnose_frontier_runtime.py --diameters 80,60 --phi-step-deg 60 --max-total-cap-plies 3 --max-states-per-depth 200 --max-frontier-size 200
```

### 2026-05-05 追加検証結果

以下は PASS 済み。

```powershell
C:\Users\Shidw\anaconda3\python.exe -m compileall src tools
C:\Users\Shidw\anaconda3\python.exe tools\verify_section_calculator.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_laminate_grammar.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_section_frontier.py
C:\Users\Shidw\anaconda3\python.exe tools\verify_span_optimizer.py
C:\Users\Shidw\anaconda3\python.exe tools\diagnose_frontier_runtime.py
```

診断ツール既定実行の代表結果:

```text
total_time_s ~= 23.3
largest_frontier ~= D87.9mm size=528
frontier size range ~= 495-528
```

### 次にやるべきこと

1. `tools/diagnose_frontier_runtime.py` で探索設定を段階的に重くする

候補:

```powershell
C:\Users\Shidw\anaconda3\python.exe tools\diagnose_frontier_runtime.py --max-total-cap-plies 5
C:\Users\Shidw\anaconda3\python.exe tools\diagnose_frontier_runtime.py --max-total-cap-plies 6
C:\Users\Shidw\anaconda3\python.exe tools\diagnose_frontier_runtime.py --phi-step-deg 30 --max-total-cap-plies 8
```

2. 近似 pruning の安全性評価ツールを作る

候補ファイル:

```text
tools/compare_frontier_pruning.py
```

比較したい条件:

```text
exact frontier
approximate_prefix_pruning=True
approximate_bin_pruning=True
```

見るべき指標:

- exact に対する近似 frontier の取りこぼし
- span optimizer の最終重量差
- たわみ制約 margin
- 計算時間短縮率

3. pruning 評価後に、`AeroStructOptimizerConfig.frontier_config` の実用設定を決める

現状の `AeroStructOptimizerConfig` は以下で、まだ暫定。

```python
FrontierConfig(max_states_per_depth=800, max_frontier_size=1500)
```

実機探索で使う前に、診断ツールと pruning 比較で根拠を作ること。
